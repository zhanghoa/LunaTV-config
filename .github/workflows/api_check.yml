name: API Check

on:
  schedule:
    # 每天北京时间凌晨1点运行 (UTC 17点)
    - cron: "0 17 * * *"
  push:
    paths:
      # 监听 LunaTV-config.json 文件变更
      - LunaTV-config.json
  workflow_dispatch:
    # 添加手动触发
    inputs:
      api_name:
        description: '搜索关键字 (用于 API 搜索功能检测)'
        required: false
        default: '你好' # 默认搜索关键字

jobs:
  check_apis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: 安装 Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: 安装依赖
        # 假设所有脚本都依赖 axios
        run: npm install axios

      - name: 运行 API 检查和报告生成脚本 (check_api.js)
        # 运行主检查脚本，生成 report.md
        run: |
          API_NAME="${{ github.event.inputs.api_name || '你好' }}"
          echo "检查 API: $API_NAME"
          # 确保这个文件名与您仓库中的实际文件名一致
          node check_api.js "$API_NAME" 

      - name: 运行 TVBox 配置转换脚本 (generate_tvbox_config.js)
        # 读取 report.md，生成 tvbox-config-healthy.json
        run: node generate_tvbox_config.js

      - name: 运行 README 更新脚本 (update_readme.js)
        # 读取 report.md，生成 README.md
        run: node update_readme.js

      - name: 提交并推送报告 (当前仓库)
        # 推送 report.md 和 README.md 到当前仓库
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 仅添加当前仓库需要的文件
          git add report.md README.md
          git commit -m "更新 API 报告和 README ($(date -d '+8 hour' +'%Y-%m-%d %H:%M:%S CST'))" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
          
      - name: 🚀 推送 TVBox 配置到目标仓库 (zhanghoa/config)
        # ⚠️ 注意：使用新的 Actions
        uses: ad-m/github-push-action@master
        with:
          # 必须使用您在 Secrets 中设置的 PAT
          github_token: ${{ secrets.PUSH_TO_OTHER_REPO }}
          
          # 目标仓库，格式为 user/repo
          repository: zhanghoa/config
          
          # 源文件路径：当前仓库中生成的配置
          source: tvbox-config-healthy.json
          
          # 目标路径：将源文件推送到目标仓库的 config/tvbox.txt
          destination: config/tvbox.txt
          
          # 目标分支
          branch: main
          
          # 提交信息
          message: "自动更新 TVBox 健康配置 [Action Run ID: ${{ github.run_id }}]"
          
      - name: 删除过期工作流记录
        # 清理工作流运行历史
        uses: Mattraks/delete-workflow-runs@main
        with:
          retain_days: 1
          keep_minimum_runs: 5
